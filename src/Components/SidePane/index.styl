.SidePane
  position fixed
  top 0
  z-index $z_global_side-pane
  overflow auto
  box(80vw, 100%)
  max-width 600px
  background-color #fff
  box-shadow 0px 0px 8px rgba(0,0,0,0.6)
  smooth-scroll()
  +below($desktop)
    max-width 320px
